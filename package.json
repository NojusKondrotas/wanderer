{
  "name": "wanderer",
  "version": "1.0.0",
  "main": "src/main/main.js",
  "description": "A lightweight experimental cross-platform note-taking app.",
  "repository": "https://github.com/NojusKondrotas/wanderer",
  "scripts": {
    "b-ejs": "node ./src/dev/build-ejs.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "fresh": "rimraf ./src/saves && cd src && mkdir saves && cd .. && electron .",
    "start-s": "npm run c-sass && electron .",
    "fresh-s": "npm run c-sass && rimraf ./src/saves && cd src && mkdir saves && cd .. && electron .",
    "c-sass": "sass ./src/main/styles.scss ./src/main/styles.css && sass ./src/main/titlebar-style.scss ./src/main/titlebar-style.css"
  },
  "keywords": [],
  "author": "Nojus Kondrotas <nojus.kondrotas23@gmail.com>",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "ejs": "^3.1.10",
    "electron": "^37.2.4",
    "rimraf": "^6.0.1",
    "sass": "^1.91.0"
  },
  "dependencies": {
    "@hurdlegroup/robotjs": "^0.12.3",
    "quill": "^2.0.3"
  }
}
